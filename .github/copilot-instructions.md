# Copilot Instructions

This project is a Playwright test framework scaffold following industry practices.

- Run `npm install` then `npx playwright install` to bootstrap the project.
- Use `npm test` to execute tests.
- Adhere to page object model and directory conventions.
